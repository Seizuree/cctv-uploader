# Camera configuration
CAM01_URL=http://192.168.1.10:80
CAM01_USER=admin
CAM01_PASS=changeme

# AWS S3
AWS_BUCKET=your-bucket-name
AWS_ACCESS_KEY_ID=AKIAxxxxxxxxxxxxxxxxxxxx
AWS_SECRET_ACCESS_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
AWS_DEFAULT_REGION=ap-southeast-1

# PostgreSQL (used by app)
# For local Docker compose, this will be overridden by container networking.
DATABASE_URL=postgresql://cctv:cctv@localhost:5432/cctv

# PostgreSQL container (docker-compose)
POSTGRES_USER=cctv
POSTGRES_PASSWORD=cctv
POSTGRES_DB=cctv

# Notes:
# - Konfigurasi kamera sekarang di database Postgres (tabel cameras)
# - Waktu pada request /export harus dalam timezone kamera (naive ISO 8601)
# - Jika berjalan di Docker, kredensial AWS dibaca dari env container ini
